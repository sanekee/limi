on:
  push:
    branches: "**"
    paths-ignore:
      - "docs/**"
  pull_request:
    branches: "**"
    paths-ignore:
      - "docs/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

name: Tests
jobs:
  test:
    env:
      GO111MODULE: on
      GOPRIVATE: github.com/${{ github.repository }}

    strategy:
      matrix:
        go-version: [1.20.x]
        os: [ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Setup Git
        env:
          TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: git config --global url."https://sanekee:${TOKEN}@github.com".insteadOf "https://github.com"
      - name: Install Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ matrix.go-version }}
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          path: ${{ github.workspace }}/src/github.com/${{ github.repository }}
      - name: Test
        env:
          working-directory: ${{ github.workspace }}/src/github.com/${{ github.repository }}
        run: |
          go get -d -t ./...
          go test $(go list ./... | grep -v /example/)
      - name: Integration Test Prep
        env:
          working-directory: ${{ github.workspace }}/src/github.com/${{ github.repository }}/example/rest
        run: |
          go run main.go
      - name: Integration Test
        env:
          working-directory: ${{ github.workspace }}/src/github.com/${{ github.repository }}/example/rest/scripts
        run: |
          sh ./test.sh
